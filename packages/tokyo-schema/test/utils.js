import moment from "moment";
import BigNumber from "bignumber.js";

import { testValue } from "../src/lib/utils";

const should = require("chai")
  .use(require("chai-as-promised"))
  .use(require("chai-bignumber")(BigNumber))
  .should();

describe("Utils", () => {
  describe("testValue", () => {

  });
});
